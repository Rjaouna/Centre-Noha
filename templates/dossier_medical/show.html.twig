{% extends 'base.html.twig' %}

{% block title %}DossierMedical{% endblock %}

{% block body %}
    <h1>DossierMedical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dossier_medical.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ dossier_medical.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dossier_medical.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ dossier_medical.createdAt ? dossier_medical.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ dossier_medical.updatedAt ? dossier_medical.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dossier_medical_index') }}">back to list</a>

    <a href="{{ path('app_dossier_medical_edit', {'id': dossier_medical.id}) }}">edit</a>

    {{ include('dossier_medical/_delete_form.html.twig') }}
{% endblock %}
