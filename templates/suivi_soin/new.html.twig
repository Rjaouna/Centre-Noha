{% extends 'base.html.twig' %}

{% block title %}Nouveau soin
{% endblock %}

{% block body %}

	<div class="container mt-4">

		{# ----------------------------------------------------- #}
		{# 🩺 HEADER — STYLE PROFESSIONNEL                     #}
		{# ----------------------------------------------------- #}
			<div class="card shadow-lg border-0 rounded-4 mb-4"> <div class="card-body d-flex justify-content-between align-items-center py-4 px-4">

				<div class="d-flex align-items-center">
					<i class="bi bi-plus-circle-fill text-success fs-1 me-3"></i>

					<div>
						<h2 class="fw-bold text-success mb-1">Nouveau soin</h2>
						<p class="text-muted mb-0">Ajouter une nouvelle consultation pour ce patient</p>
					</div>
				</div>

				
			</div>
		</div>


		{# ----------------------------------------------------- #}
		{# 🧾 CARTE FORMULAIRE — STYLE SOFT MEDICAL            #}
		{# ----------------------------------------------------- #}
			<div class="card shadow-sm border-0 rounded-4"> <div class="card-body p-4">

				<h4 class="fw-semibold text-primary mb-4 d-flex align-items-center">
					<i class="bi bi-journal-medical me-2"></i>
					Informations du soin
				</h4>

				<div class="soft-form">
					{{ include('suivi_soin/_form.html.twig', {
                    button_label: 'Enregistrer'
                }) }}
				</div>

			</div>
		</div>

	</div>


	{# ----------------------------------------------------- #}
	{# 🎨 CSS DOUX POUR UN LOOK PREMIUM                      #}
	{# ----------------------------------------------------- #}
		<style>
.soft-form .form-control,
.soft-form select {
	border-radius: 0.8rem;
	padding: 0.75rem 1rem;
	border: 1px solid #dcdcdc;
	transition: all 0.2s ease;
}
.soft-form .form-control:focus {
	border-color: #52a5ff;
	box-shadow: 0 0 0 0.25rem rgba(0, 123, 255, .15);
}

.soft-form button {
	font-size: 1.1rem !important;
	padding: 0.75rem 2rem !important;
}

.card {
	transition: 0.2s ease;
}
.card:hover {
	transform: translateY(-2px);
	box-shadow: 0 1rem 1.6rem rgba(0, 0, 0, 0.1);
}
</style>{% endblock %}

