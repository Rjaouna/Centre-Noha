{% extends 'base.html.twig' %}

{% block title %}Détail du soin
{% endblock %}

{% block body %}
	<div class="container-fluid mt-4">

		{# ------------------------------------------------------- #}
		{# 🌟 HEADER DE PAGE LUXE STYLE "CARTE MÉDICALE"           #}
		{# ------------------------------------------------------- #}
			<div class="card shadow-lg border-0 rounded-4 mb-4"> <div class="card-body py-4 px-4">

				<div class="d-flex justify-content-between align-items-center">

					<div class="d-flex align-items-center">
						<div class="bg-danger bg-gradient text-white p-3 rounded-circle shadow-sm me-3">
							<i class="bi bi-heart-pulse fs-3"></i>
						</div>

						<div>
							<h2 class="fw-bold text-danger mb-1">Détail du soin</h2>
							<p class="text-muted mb-0">Visualisation complète du soin réalisé</p>
						</div>
					</div>

					<a href="{{ path('app_suivi_soin_index', {'id': suivi_soin.patient.id}) }}" class="btn btn-outline-secondary btn-lg rounded-pill shadow-sm px-4">
						<i class="bi bi-arrow-left me-2"></i>
						Retour à l'historique
					</a>

				</div>

			</div>
		</div>


		{# ------------------------------------------------------- #}
		{# 🌟 CARTE PRINCIPALE : PRÉSENTATION MODERNE              #}
		{# ------------------------------------------------------- #}
			<div class="card shadow-lg border-0 rounded-4 p-4"> <div class="row mb-4 align-items-center">
				<div class="col">
					<h4 class="fw-bold text-primary mb-0 d-flex align-items-center">
						<i class="bi bi-journal-medical fs-3 me-2 text-primary"></i>
						Informations du soin
					</h4>
				</div>

				<div class="col text-end">
					<a href="{{ path('app_suivi_soin_edit', {'id': suivi_soin.id}) }}" class="btn btn-primary px-4 rounded-pill shadow-sm">
						<i class="bi bi-pencil-square me-2"></i>
						Modifier le soin
					</a>
				</div>
			</div>


			{# ------------------------------------------------------- #}
			{# 🌟 GRILLE DES INFORMATIONS                            #}
			{# ------------------------------------------------------- #}
				<div
				class="row g-4"> {# 🧍 NOM DU PATIENT #}
				<div class="col-md-6">
					<div class="info-box border-start border-4 border-primary bg-light rounded-4 shadow-sm p-4 h-100">
						<p class="fw-semibold text-secondary mb-1">
							<i class="bi bi-person-fill text-primary me-2"></i>
							Patient concerné
						</p>
						<p class="fs-4 fw-bold text-dark">{{ suivi_soin.patient.nom }}</p>
					</div>
				</div>

				{# 📋 DESCRIPTION DU SOIN #}
				<div class="col-md-6">
					<div class="info-box border-start border-4 border-success bg-light rounded-4 shadow-sm p-4 h-100">
						<p class="fw-semibold text-secondary mb-1">
							<i class="bi bi-clipboard2-pulse text-success me-2"></i>
							Description du soin
						</p>

						<p class="fs-5 fw-bold text-dark">
							{{ suivi_soin.diagnostic }}
						</p>
					</div>
				</div>

				{# 📅 DATE DU SOIN #}
				<div class="col-md-6">
					<div class="info-box border-start border-4 border-warning bg-light rounded-4 shadow-sm p-4 h-100">
						<p class="fw-semibold text-secondary mb-1">
							<i class="bi bi-calendar-check text-warning me-2"></i>
							Date du soin
						</p>

						<p class="fs-4 fw-bold text-dark">
							{{ suivi_soin.createdAt|date('d/m/Y à H:i') }}
						</p>
					</div>
				</div>

				{# ✏️ DERNIÈRE MODIFICATION #}
				<div class="col-md-6">
					<div class="info-box border-start border-4 border-info bg-light rounded-4 shadow-sm p-4 h-100">
						<p class="fw-semibold text-secondary mb-1">
							<i class="bi bi-pencil text-info me-2"></i>
							Dernière mise à jour
						</p>

						<p class="fs-5 fw-bold text-dark">
							{{ suivi_soin.updatedAt|date('d/m/Y à H:i') }}
						</p>
					</div>
				</div>

			</div>

		</div>
	</div>

	<style>
		/* 🌟 Optionnel : amélioration globale pour un rendu premium */
		.info-box {
			transition: 0.2s ease-in-out;
		}
		.info-box:hover {
			background: #ffffff !important;
			transform: translateY(-3px);
			box-shadow: 0 0.6rem 1.2rem rgba(0, 0, 0, 0.12) !important;
		}
	</style>

{% endblock %}

