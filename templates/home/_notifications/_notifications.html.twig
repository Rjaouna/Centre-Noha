	<div id="notifications" class="notif-fixed"></div>





<style>
.notif-item {
    opacity: 1;
    transition: opacity 0.7s ease-out, transform 0.7s ease-out;
}

.notif-item.fade-out {
    opacity: 0;
    transform: translateY(15px);
}

/* Conteneur des notifications */
.notif-fixed {
    position: fixed;
    bottom: 20px;
    left: 20px;
    width: 320px;
    max-height: 300px;
    overflow-y: auto;
    z-index: 99999;
}

/* Style d'une notification */
.notif-item {
    margin-bottom: 10px;
    border-left: 5px solid #fff;
}

/* Scrollbar douce */
.notif-fixed::-webkit-scrollbar {
    width: 6px;
}
.notif-fixed::-webkit-scrollbar-thumb {
    background: #ffffff77;
    border-radius: 10px;
}

</style>

