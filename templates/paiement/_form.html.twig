{{ form_start(form, {'attr': {'class': 'mt-3'}}) }}

<div class="form-floating mb-3">
	{{ form_widget(form.prixTotal, {'attr': {'class': 'form-control', 'placeholder': 'Prix total'}}) }}
	<label for="{{ form.prixTotal.vars.id }}">Prix total (DH)</label>
</div>

<div class="form-floating mb-3">
	{{ form_widget(form.montantPaye, {'attr': {'class': 'form-control', 'placeholder': 'Montant payé'}}) }}
	<label for="{{ form.montantPaye.vars.id }}">Montant payé (DH)</label>
</div>

<div class="form-floating mb-3">
	{{ form_widget(form.reste, {'attr': {'class': 'form-control', 'placeholder': 'Reste à payer'}}) }}
	<label for="{{ form.reste.vars.id }}">Reste à payer (DH)</label>
</div>

<div class="form-floating mb-4">
	{{ form_widget(form.typePaiement, {'attr': {'class': 'form-select'}}) }}
	<label for="{{ form.typePaiement.vars.id }}">Type de paiement</label>
</div>

<button class="btn btn-success btn-lg rounded-pill px-4 shadow-sm">
	<i class="bi bi-check2-circle me-2"></i>
	{{ button_label|default('Valider') }}
</button>

{{ form_end(form) }}

